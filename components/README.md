# components/

Web Components de retals. un archivo por componente.

ver `CLAUDE.md` para el contrato y `_template.js` para empezar uno nuevo.

cuando se implementen, listar aquí cada uno con un link a su docs en `/docs/`.
